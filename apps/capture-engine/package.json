{
  "name": "capture-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Méra OS — Local HTTP server on Mac Mini. Bridges Capture One sessions to the Kiosk tablet.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
